export const BAG_IT_EMOJI = String.fromCodePoint(0x1f6cd, 0xfe0f);
export const MY_BAG_EMOJI = String.fromCodePoint(0x1f9fa);

export const BAG_IT_LABEL = 'Bag It';
export const MY_BAG_LABEL = 'My Bag';

/**
 * Custom orders are marked with scissors, everywhere, with no words.
 *
 * The market cards previously carried the words "Custom quote" inside a narrow
 * price chip, which truncated to "Custo..." — a label so clipped it stopped
 * being a label. A mark reads at any card width and needs no room of its own.
 */
export const CUSTOM_ORDER_EMOJI = String.fromCodePoint(0x2702, 0xfe0f);
export const CUSTOM_ORDER_LABEL = 'Custom order';
